"""Синхронизация между устройствами (опциональная, по локальной сети).

Свой движок: op-log с LWW-семантикой поверх mDNS-обнаружения и взаимного TLS.
Без серверов и интернета. См. docs/sync-and-mobile-plan.md.

Модуль самодостаточен и не импортируется UI, пока синхронизация выключена
(config.sync_enabled()). Зависит от cryptography (ключи/cert/TLS); zeroconf и
segno подключаются на своих шагах.
"""
